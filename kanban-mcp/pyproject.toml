[project]
name = "aglaya-kanban-mcp"
version = "0.1.0"
description = "MCP rail (stdio) to operate AGLAYA Kanban Desk via its API — captain never sees secrets."
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.2.0",
    "httpx>=0.27",
]

[tool.setuptools]
py-modules = ["server"]
