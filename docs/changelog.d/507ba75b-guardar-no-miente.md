Fixed

- **Un fallo al guardar, mover o borrar ya no pasa en silencio.** Tarjeta `507ba75b`, hallazgo A-30 de la auditoría del 24-sep-2026.
  - **Lo que había, medido y más matizado que la tarjeta:** al **mover**, `useBoardData.moveCard` se tragaba el error (`catch { load() }`), así que el modal se cerraba igual y lo escrito desaparecía de la pantalla sin haber llegado al servidor. Al **actualizar**, la promesa se rompía y nadie la escuchaba: el modal se quedaba abierto **sin una palabra**.
  - Ahora el modal **no se cierra** si algo falla, lo escrito sigue a la vista, y un aviso dice qué pasó **con el motivo del servidor dentro**. El aviso **no se va solo**: uno que se desvanece a los tres segundos es casi lo mismo que no avisar.
  - **El hook deja de tragarse el fallo al mover:** deshace el movimiento optimista **y vuelve a lanzar**. Deshacer sin avisar es la mitad del trabajo — la tarjeta volvía sola a su sitio y nadie explicaba por qué.
  - **Borrar una columna con tarjetas** contesta 409, y ahora se espera al servidor antes de cerrar la ventana: se ve el motivo en vez de prometer un borrado que no ocurre.
  - **«No se pudo mirar» deja de verse igual que «no hay nada»:** un fallo al cargar los espacios de trabajo pintaba el mismo estado vacío que una cuenta recién creada. Ahora dice que no se pudieron cargar, con el motivo y un botón para reintentar. Es el mismo defecto que ya se cerró en la búsqueda del servidor (`1753729e`).
  - **Cinco mutaciones, cada una tumba su caso**, y dos huecos que destaparon ellas y no la lectura: la prueba del Board **no ejecutaba el hook** (pasa `onMoveCard` como prop), así que devolver el `catch` viejo seguía en verde — se añadió prueba propia del hook; y en ella, `expect(act(...)).rejects` **no medía nada**, porque `act` se come el rechazo.
