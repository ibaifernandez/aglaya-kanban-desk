Added

- **El cliente ya se puede probar, y sus pruebas corren en CI.** Tarjeta `97307036`.
  - Hasta hoy no había **ninguna**: `npm test` era solo `jest server/tests`, y en `client/` no había corredor, ni entorno con pantalla, ni un solo fichero de prueba. Tres tarjetas abiertas (`507ba75b`, `48946335`, `1f1eb472`) piden comprobar lo que ve el usuario y no se podía medir.
  - **Lo mínimo y nada más:** `vitest` con `jsdom` y `@testing-library/react`, configuración propia (`client/vitest.config.js`, aparte del `vite.config.js` que construye producción) y un arranque que limpia el DOM entre pruebas — sin eso, una prueba hereda la pantalla de la anterior y pasa por el motivo equivocado. No se pide cobertura de nada.
  - **Prueba piloto:** `ColumnPickerModal`, 4 casos que miran lo que el usuario ve. Cuatro mutaciones del componente, cada una tumba su caso.
  - **Y un guardián nuevo, `cliente-probado-guard.sh`**, porque el riesgo cambia de sitio en cuanto el entorno existe: no es que falten pruebas, es que **dejen de correr sin que nadie lo note**. Exige que haya pruebas, que el guion `test` llame de verdad a vitest, y que CI lo invoque **en el mismo paso** que fija el directorio del cliente. Sello: 9 casos, incluido el de las dos señales sueltas en pasos distintos. Quitando el paso de CI, el guardián y su sello se ponen rojos.
  - **Devuelta por el vigilante, con razón:** el guardián comprobaba que el paso **existiera**, no que pudiera poner nada en rojo. Cinco formas de dejarlo puesto y neutralizado pasaban en verde. La que devolvió la PR no es retorcida sino lo contrario: **`continue-on-error: true` es el idioma que ese mismo fichero usa seis líneas más arriba** para los guardianes, así que copiarlo al paso de las pruebas es un descuido de una línea — y deja las pruebas corriendo **sin gobernar nada**. Ahora el guardián mira **cómo** está escrito el paso: `if:`, `continue-on-error:` o `--passWithNoTests` lo ponen rojo. Cuatro casos nuevos en el sello (13), tres mutaciones del guardián, tres rojas.
  - **Devuelta una segunda vez, y la que más pesa es la del guion:** el control que exige que `test` llame a vitest existía exactamente para impedir un falso verde —su comentario decía que «un test que imprima ok y salga con 0 es el falso verde perfecto»— y aun así pasaba `vitest run … || echo ok`: **miraba que el guion nombrara a vitest, no que respetara su veredicto**. Igual en el workflow con `npm test || true`. Ahora un comando que se traga el veredicto (`|| true`, `|| echo`, `; true`, `|| exit 0`) pone rojo **en los dos sitios**, y cada sitio tiene su caso en el sello (16): fijar solo uno dejaba el otro abierto, que es esta misma lección en pequeño.
  - **Tercera vuelta, y ahí el vigilante propuso cambiar de estrategia en vez de traer otra lista** — `npm test | tee salida.log` sale 0 porque en una tubería manda el último mandato, y `npm test &` sale 0 en el acto. **La regla se invierte:** el paso ejecuta **exactamente** `npm test` o `npm run test`, y el guion del cliente **empieza** por `vitest run` y no encadena nada. Con eso caen también las formas que ninguno de los dos ha imaginado, y el guardián deja de depender de la imaginación de quien revisa. El precio, dicho: una forma legítima nueva tendrá que pasar por aquí y explicarse — que es el precio correcto.
  - **Y el sello fija formas legítimas, no solo ataques:** `npm run test` y un guion con flags (`--reporter dot`) tienen que seguir pasando. Sin eso, la regla invertida se podría «cumplir» prohibiéndolo todo, y un guardián que no deja trabajar se desactiva a la primera. **23 casos.**
  - **Y una ironía que conviene dejar escrita:** el código nuevo de esta obra usaba `printf … | grep -q`, que es justo lo que `pipefail-guard` prohíbe —una tubería hacia un lector que sale antes de tiempo—, y **puso el CI en rojo**. La obra que enseña al CI a no tragarse veredictos **se tragaba uno al construirse**. Va con `<<<`.
  - **Un fallo mío que destapó el propio sello:** el mensaje de error llevaba comillas invertidas dentro de comillas dobles, así que **ejecutaba mandatos** al construirse. Corregido.
  - **Formas legítimas que quedan prohibidas, dichas en la cabecera para que nadie crea que es un fallo:** un prefijo en el guion (`npx vitest run …`, `cross-env CI=true vitest run …`) y banderas en el paso (`npm test --silent`). Hoy no se usan; el día que hagan falta, ensanchar la regla es parte de ese cambio y se ve en la revisión. Admitir banderas en el paso reabriría `-- --passWithNoTests`.
  - **Y lo que no puede medir, escrito en su cabecera:** qué jobs son **obligatorios** para fusionar lo custodia el *ruleset* de GitHub, no el repositorio, así que mover el paso a un job que nadie exige pasaría por aquí en verde. Hoy vive en `client-build`, que sí es exigido.
  - **Declarado, no escondido:** las dependencias nuevas suman 2 avisos moderados de `npm audit` respecto a `main` (9 frente a 7), y los dos cuelgan de la versión de `vite` que ya estaba. Con `vitest` 3 en vez de 2 no queda ninguno crítico.
