Fixed

- **La agregación de `B-03` no agregaba el tráfico del riel.** El defecto lo introdujo el arreglo anterior (`5690321`), un día antes. Tarjeta `b9b58f10`, hallazgo del vigilante dentro de `4ae4ffe1` y reproducido por el capataz.
  - **Qué fallaba:** la clave era `req.path` **literal**, y el tráfico del riel lleva identificadores dentro (`/api/boards/<uuid>/columns`). Cada petición producía **una clave nueva**, así que la ventana no agrupaba nada: **1.000 peticiones → 1.000 eventos**, contra 1.000 → 1 en una ruta fija. La ventana funcionaba; **lo que no funcionaba era la clave**.
  - **Y el acumulador no se podaba nunca** — ni un `delete`, ni un `clear`. Agregar bien sin podar sigue dejando un `Map` que solo crece: son las dos mitades del mismo arreglo.
  - **Se agrupa por la FORMA de la ruta** (`/api/boards/:id/columns`), con los identificadores —UUID y numéricos— sustituidos. **`req.route` no se usa porque no existe en ese punto**: comprobado, no supuesto — el monitor corre en un `app.use` temprano, antes del enrutado, y allí `req.route` es `undefined` y `req.baseUrl` es cadena vacía.
  - **La etiqueta que agrupa en Sentry es la forma; la ruta concreta viaja como `path_ejemplo`.** Una etiqueta distinta por petición es el mismo defecto por la puerta de la cardinalidad.
  - **La poda corre al emitir**, no con un temporizador: un temporizador mantendría vivo el proceso por un contador de ruido. Y hay **techo de 500 formas**: si se pasa, se reinicia el recuento **y se dice** — vaciar pierde algún contador, no vaciar pierde el proceso, y callarlo sería lo peor de los dos. Si eso llega a pasar, **el hallazgo es que algo genera rutas sin límite**, no el tamaño.
  - ⚠️ **Hoy no hacía daño y aun así se arregla:** sin dominio propio el monitor está inerte y no llega a esa rama. Pero **el runbook del dominio manda encender esa variable**, así que era una mecha con la instrucción de encenderla ya escrita — y lo que encendería es el mismo incidente que dejó ciega a la flota.
  - **La retención se mide por comportamiento, no por memoria:** cuántas claves quedan vivas, observable desde fuera. **La cifra de «+45 MB» del hallazgo no se pudo reproducir** —tres intentos del capataz: 0,0 · 0,2 · 60,4— y no se cita como dato: el arreglo no depende de ella.
  - 22 casos en el banco; **siete mutaciones, siete rojas**, incluidas la vuelta a la clave literal, quitar la poda, quitar el techo y vaciar sin avisar.
