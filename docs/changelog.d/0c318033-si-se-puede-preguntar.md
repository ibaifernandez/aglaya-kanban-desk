Fixed

- **`CLAUDE.md` decía que no se puede consultar la base desde aquí, y sí se puede: mandaba a la cuadrilla por caminos caros.** Tarjeta `0c318033`.
  - La frase retirada: «el MCP de Supabase de esta máquina apunta a **otra organización**». **Es falsa.** El conector alcanza el proyecto de producción (`AGLAYA Kanban Desk`) y contesta consultas sueltas: políticas RLS, recuentos, el contenido de una fila. Medido hoy en tres tarjetas (`6df9d529`, `22ecfa81`, `4f4e6e2b`) — y el vigilante seguía dando por imposible lo que yo acababa de medir con esa vía, que es exactamente el coste del defecto.
  - **Se escribe con su advertencia**, porque sin ella el arreglo abre una puerta peor: `execute_sql` **ejecuta cualquier SQL, también escrituras y DDL**. Para mirar, `SELECT`; para cambiar, migración en `docs/schema/` aplicada por el Operador.
  - **Eran dos documentos, no uno**, y lo vio el vigilante: `ARCHITECTURE.md` abría con la misma frase falsa —«ningún papel automático puede abrir la base… el MCP está autenticado contra otra organización»— **en la sección escrita para agentes**, que es la que se lee justo cuando hace falta una lectura. Retractada ahí también.
  - **Y el guardián no la veía porque iba partida en dos renglones:** buscaba línea a línea. Ahora normaliza a una sola línea, y el sello tiene ese caso exacto.
  - `ARCHITECTURE.md` gana la sección del conector y corrige dos cosas que daba por imposibles: una consulta suelta y «¿qué políticas RLS hay puestas?».
  - **`base-consultable-guard.sh` gana una tercera dirección**: rojo si el documento deja de nombrar la vía del conector, y rojo si vuelve a negarla. Con su límite dicho: que el conector siga funcionando **no** está en ningún fichero del repositorio, así que eso no lo puede medir.
  - **Y una lección de guardián:** la primera versión de ese control se ponía roja contra su propio texto —el párrafo que prohíbe negar la vía contiene la negación— y contra la retractación de `CLAUDE.md`, que **cita** la frase falsa para desmentirla. Ahora lo que va entre marcas de retractado no se mira, y el sello tiene un caso que lo fija. Sello: 20 casos; tres mutaciones del guardián, tres rojas.
