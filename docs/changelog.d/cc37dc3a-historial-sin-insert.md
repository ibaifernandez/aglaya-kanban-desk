Changed

- **El historial de descripciones deja de ser escribible por quien lo genera.** Tarjeta `cc37dc3a`. Se escribe `docs/schema/migration-historial-sin-insert.sql`, que retira `INSERT` a `authenticated` sobre `card_description_history` — el recorte anterior (`2c034471`) quitó `UPDATE` y `DELETE`, y dejó dicho que el mismo argumento valía para `INSERT`.
  - **`INSERT` no es «menos» que `UPDATE`: es otra cosa.** No deja alterar lo guardado, deja **añadir**. Un historial al que se le pueden inyectar filas no es menos recuperable, es **menos creíble** — se puede fabricar una versión anterior que nunca existió, y quien lo lea no tiene con qué distinguirla, porque **el historial no distingue autor**: todo sale como `Kanban Rail (orchestrator)`.
  - **Radio medido para `INSERT`, no heredado** —la tarjeta avisaba de que se midió para `UPDATE`/`DELETE` y no se había repetido—: el único `INSERT` de la nave sobre esa tabla es `server/routes/cards.js:449` y usa `service_role`; el navegador nunca llama `.from(...)` (los únicos aciertos en `client/src` son `Array.from`). Cero caminos rotos.
  - ⏳ **Pendiente del Operador**, como su hermana: aplicar SQL no es acción de un agente.
- **Y el hallazgo que cambió la tarjeta: `migration-historial-append-only.sql` decía «PENDIENTE DE APLICAR» y llevaba diecisiete días aplicada.**
  - Nadie mintió: se aplicó y nadie volvió al fichero. **El estado lo custodia la base y estaba escrito a mano en un comentario.**
  - Constatado sin tocar la base, y por quien no la aplicó: `grants-guard` compara **exacto** lo que concede la base contra lo que declara el esquema, y lee la excepción por tabla desde el 8-ago (`dc291a8`). Con la migración sin aplicar estaría **rojo**. Está **verde desde el 9-ago** (corrida `31313369793`, 2026-08-09T12:28:26Z) y lo seguía el 25-ago a las 16:22:21Z (corrida `32871541732`). Se aplicó entre el 8 y el 9 de agosto; **quién lo hizo no se sabe y no se inventa.**
  - **Costó una decisión.** `cc37dc3a` planificaba sobre esa línea —«mientras no esté aplicada, las dos caben en una sola ejecución del Operador»—, y esa ventana llevaba diecisiete días cerrada. Ahora son dos ejecuciones porque ya lo eran, no porque se haya tardado.
- **El orden entre aplicar y declarar queda vigilado, y no por acordarse.** El esquema **sigue declarando `SELECT, INSERT` a propósito**: declararlo antes de aplicar pondría `grants-guard` **rojo en `main`** —con razón— hasta que alguien ejecutara el SQL, y un guardián que vive en rojo se normaliza hasta que deja de mirarse. `server/tests/historial-sin-insert.test.js` se pone rojo en cuanto la migración diga «APLICADA» y el esquema siga diciendo `INSERT`. **Las dos puntas cubiertas**, y la prueba estrena verde por la rama de «pendiente».
  - Ocho casos; **cinco mutaciones, cinco rojas** — incluido el olvido real (poner «APLICADA» sin tocar el esquema) y su inverso (adelantar el esquema antes de aplicar).
