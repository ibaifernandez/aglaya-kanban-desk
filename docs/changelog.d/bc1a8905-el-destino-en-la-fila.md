Added

- **La fila que escribe la puerta interna ya tiene quien mire su destino.** Quitar `board_id` o `column_id` del `insert` pone roja la suite, y el rojo **dice que es el `insert`**. Tarjeta `bc1a8905`.
  - El acuse no valía de testigo: se construye a partir de las variables locales, así que **seguía diciendo la verdad aunque la fila ya no la tuviera**. Dos superficies, una vigilada.
  - Sin eso, una tarjeta podía existir en `cards` sin tablero ni columna, con `201` por respuesta y sin aparecer en ningún tablero: la familia «nace invisible», un piso más abajo — no aterriza mal, **no aterriza**.
  - **Y entra como octava mutación de `puerta-interna.mutation.sh`**, que ahora corre nueve y las nueve mueren. Se dejó fuera hasta tener la aserción a propósito: una batería que estrena en rojo se normaliza.
