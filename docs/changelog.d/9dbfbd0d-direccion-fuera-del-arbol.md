Fixed

- **La dirección personal del Operador salía del árbol de un repositorio público, y el guardián que debía impedirlo estaba en verde por decisión, no por hueco.** Tarjeta `9dbfbd0d`.
  - **No eran cuatro ubicaciones: eran once**, en nueve ficheros —documentación de arquitectura, permisos, seguridad, producto, backlog, incidencias, changelog, los dos artefactos de la auditoría de mayo— más **una tecleada dentro de un guardián** (`scripts/rail-blindspot.sh`) y **una en una fixture de test**. Y el dominio legado escondía **dos más** que solo aparecieron al retirar la declaración.
  - ⚠️ **El guardián no era `docs-guard`, y su alcance no era el problema.** Quien vigila esto es **`email-guard`**, que ya deriva su alcance de **todos los ficheros versionados** —no de una lista de dos—. Estaba verde porque `ibaifernandez.com` y `lfi.la` estaban **declarados como dominios enteros**, con su motivo escrito: *«publicar su propia dirección es decisión suya y ya la tomó»*. **Un `dominio` abre ese dominio para siempre y para cualquier dirección futura**, así que cada vez que alguien la escribía en un documento nuevo, entraba en silencio. El defecto era la declaración, no la cobertura.
  - **Las dos declaraciones se retiran** —el Operador tomó la decisión contraria el 2026-09-12— y en su sitio queda escrito qué había y por qué se fue, para que nadie lo reponga creyendo que arregla algo.
  - **Dónde queda el dato:** el custodio es la tabla `users`. Los documentos pasan a nombrar el **papel** («el operador humano», «la cuenta superadmin»), que es lo que decían de verdad. Los registros históricos —incidencias, changelog, informe de auditoría— **no se borran: se redactan**, para no perder el hecho que narran.
  - **Y el guardián del riel deja de identificar al dueño por su dirección y lo hace por su papel** (`role = 'superadmin'`, excluyendo la cuenta de rol del riel). Era lo que la condición quería decir: el dueño no es esa dirección, es quien tiene ese papel. **Consecuencia declarada:** si algún día hay un segundo superadmin humano, el alcance se ensancha solo.
  - **Cerrado con caso propio y tres mutaciones rojas:** reabrir el dominio personal, reabrir el legado, o vaciar el fichero de declaraciones ponen la batería en rojo. Sin él, una línea de tres palabras reabría las once ubicaciones de golpe **sin que nada se pusiera rojo**, que es exactamente cómo se llegó aquí.
  - **Medido en las dos direcciones:** el árbol versionado devuelve **cero** direcciones sin declarar, y añadir la dirección a un fichero nuevo cualquiera pone `email-guard` en rojo (exit 1). Antes no lo ponía.

Known

- **Queda una ubicación que no se ha tocado y necesita decisión: el DPA de Railway (`docs/legal/dpas/railway-dpa-2026-05-27.pdf`) contiene la dirección personal cuatro veces.** Es un PDF firmado y archivado como prueba de un acuerdo de encargado: redactarlo altera un artefacto legal, y retirarlo del árbol quita esa prueba de donde el registro la busca. **`email-guard` no lo ve** —salta los binarios al no poder decodificarlos, y lo declara en su cabecera—, así que su verde no cubre este fichero. Los otros seis acuerdos archivados están limpios: comprobado uno a uno.
