Fixed

- **La política publicada terminaba con una lista de «Acciones pendientes» ya hechas, y el checklist del Operador seguía dando órdenes de mayo.** Tarjeta `c2a41b7f`.
  - **La política** decía «pendiente: publicar esta política como URL pública» **en la misma URL donde está publicada**, y remitía al checklist para aceptar DPA ya aceptados. La sección se retira: una lista de tareas internas no pinta nada en el documento con el que un titular ejerce sus derechos. **No sube versión:** no cambia nada de lo que se declara al titular.
  - **`docs/operator-checklist.md` se borra**, no se conserva como acta, por la regla del Operador —«un documento que acumula polvo e induce a error no se conserva»— y porque no le quedaba nada vivo: del 1 al 8 hecho; el 11 (cron de retención) era para unos plazos retirados en la política 1.3 y está aparcado en `9167d685`; el 9 y el 10 no tenían dueño ni tarjeta. Su paso 6 mandaba **guardar el DPA firmado en `docs/legal/dpas/`**, que es exactamente lo que metió la dirección del Operador en el repositorio público.
  - **La evidencia no se pierde:** vive en el historial de git. Las referencias desde la auditoría de mayo y su roadmap apuntan ahora al último commit donde existe (`17289ad`). `docs-guard` pierde la exclusión de un fichero que ya no está, y `email-guard` las tres declaraciones que solo existían por él.
  - ⚠️ **Y al tirar de ese hilo salió que el HTML de la política no se genera a mano**, como afirmaban dos comentarios de `politica-publicada.test.js`: lo genera `client/scripts/build-legal-pages.cjs` desde el markdown, en el `prebuild` del cliente. `netlify.toml` citaba además una ruta que no existe. Medido regenerándolo: la salida y el HTML versionado solo diferían en espacios. Los tres sitios dicen ahora la verdad, y **el markdown es la fuente**.
