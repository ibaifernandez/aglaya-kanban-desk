Changed

- **El runbook del dominio dice ahora qué defectos arma al encender el monitor B-03.** Tarjeta `a19afbc4`, hallazgo del capataz auditando `b9b58f10`.
  - **El paso existía y estaba incompleto:** mandaba configurar `PUBLIC_API_HOST` y comprobar que desaparece la línea `INERTE`. Nada más. Quien lo ejecute —meses después, probablemente en el mismo turno en que crea el dominio— **encendía dos defectos conocidos sin saberlo**.
  - **1 · El recuento de repeticiones puede mentir a la baja.** La poda borra la clave caducada con su contador dentro, y la dispara **cualquier otra ruta** al emitir: si eso pasa en medio, el siguiente evento declara **0 repeticiones donde hubo diez**. Reproducido por mí sobre `28232cb`. Es daño de lectura, no de cuota: el aviso llega, el volumen sale subestimado.
  - **2 · El techo de claves acota memoria, no eventos.** Un cliente con formas de ruta irrepetibles genera un evento por forma: medido, **20.000 formas → 20.000 eventos** con 461 claves vivas. La agregación protege del tráfico repetido —el incidente del 1-sep— no de un patrón que nunca repite forma. **Preexistente**, no introducido por el arreglo de la agregación.
  - ⚠️ **No se arreglan, y eso es la decisión, no una omisión.** Los dos están detrás del mismo interruptor, y hoy el monitor arranca `INERTE`: arreglarlos ahora sería construir para un mundo que no existe. Lo que no puede pasar es que se enciendan **a ciegas**.
  - ⚠️ **Y el aviso es el PASO 1, no una sección debajo.** La primera versión lo dejaba tras el cierre de la lista numerada, así que quien ejecuta —que ejecuta la lista— podía no pasar por él: su propio título decía «conviene leer», y *conviene* no es *hace falta*. **Era el mismo defecto que esta tarjeta cierra, cometido dentro de su arreglo**: información correcta colocada donde el que actúa puede no llegar. Ahora leerlo y decidir es el primer paso, y configurar la variable es el segundo.
  - **El aviso va donde está el gatillo.** Un runbook se lee justo antes de ejecutar; una tarjeta en «Hecho» no se relee nunca. Con el criterio para decidir allí mismo: si solo importa la señal «hay tráfico fuera del dominio», los dos defectos son tolerables; si se va a **contar** con el número, o la API queda expuesta a clientes que no controlas, se arreglan primero.
